This is `minishort`.

It expects a redis database on localhost. Fix that if it's elsewhere.

Generate a key with `redis-cli`:

	sadd keys.yourdomain randomstringhere

Then add URLs using either Google's URL shortener API, or using the form.
